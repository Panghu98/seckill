<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.panghu.seckill.mapper.UserMapper">
    <select id="getById" parameterType="long" resultType="com.panghu.seckill.bean.User">
        select  * from miaosha_user <where> `id` = #{id}</where>
    </select>
</mapper>